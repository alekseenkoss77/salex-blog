<% tag_cloud(tags, 'label-cloud') do |tag, style| %>
  <a href="<%= tagged_post_path(tag_id: tag.id) %>">
    <span class="name"><%= tag.name %></span>
    <span class="count"><%= tag.taggings_count %></span>
  </a>
<% end %>